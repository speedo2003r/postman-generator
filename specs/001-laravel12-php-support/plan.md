# Implementation Plan: Laravel 12 & PHP 8.2–8.4 Compatibility

**Branch**: `001-laravel12-php-support` | **Date**: 2026-02-21 | **Spec**: [spec.md](spec.md)  
**Input**: Feature specification from `specs/001-laravel12-php-support/spec.md`

## Summary

Expand the `laravelgenerators/postman-generator` package to officially declare compatibility with Laravel 12 and PHP 8.4 (alongside existing support for Laravel 11 and PHP 8.2/8.3). The change touches **three files only**: `composer.json`, `.github/workflows/tests.yml`, and `README.md`. No application source code changes are required.

## Technical Context

**Language/Version**: PHP ^8.2 (8.2, 8.3, 8.4)  
**Primary Dependencies**: `illuminate/support`, `illuminate/http`, `illuminate/routing` (^11.0|^12.0); `orchestra/testbench` (^9.0|^10.0 dev); `pestphp/pest` (^3.0 dev)  
**Storage**: N/A  
**Testing**: Pest 3.x via `orchestra/testbench` (v9 for L11, v10 for L12)  
**Target Platform**: Laravel package (runs inside host Laravel application)  
**Project Type**: Single — library package  
**Performance Goals**: N/A (compatibility change only)  
**Constraints**: Must not break Laravel 11 compatibility; PHP minimum stays at 8.2  
**Scale/Scope**: 3 files changed; 6 CI matrix jobs after change

## Constitution Check

*Constitution is not yet populated for this project — no gates to enforce. All changes are minimal and additive.*

## Project Structure

### Documentation (this feature)

```text
specs/001-laravel12-php-support/
├── plan.md              ✅ This file
├── research.md          ✅ Phase 0 — dependency version decisions
├── data-model.md        ✅ Phase 1 — configuration entities & change table
├── quickstart.md        ✅ Phase 1 — local testing guide
└── tasks.md             🔲 Phase 2 — generated by /speckit.tasks
```

### Source Code (repository root)

```text
composer.json                          ← illuminate/* constraints + dev deps
.github/workflows/tests.yml            ← CI matrix (PHP + Laravel versions)
README.md                              ← Supported versions documentation

src/                                   ← NO CHANGES (18 files, all compatible)
tests/                                 ← NO CHANGES (existing tests cover all matrix cells)
```

**Structure Decision**: Single-project library layout. No new directories or files created in `src/` or `tests/`. All changes are in configuration and documentation files at the repository root.

## Implementation Details

### Change 1 — `composer.json`

**What changes**:

| Package | From | To |
|---------|------|----|
| `illuminate/support` | `^11.0` | `^11.0\|^12.0` |
| `illuminate/http` | `^11.0` | `^11.0\|^12.0` |
| `illuminate/routing` | `^11.0` | `^11.0\|^12.0` |
| `pestphp/pest` | `^2.0` | `^3.0` |
| `orchestra/testbench` | `^9.0` | `^9.0\|^10.0` |

**Why Pest 2→3**: Pest 2 is incompatible with the `orchestra/testbench` 10.x dependency tree. Pest 3 requires PHP ^8.2 — consistent with our PHP constraint. Test DSL API is backward-compatible.

**Why testbench ^9.0|^10.0**: Testbench 9.x serves Laravel 11; testbench 10.x serves Laravel 12. The OR-constraint lets Composer pick the right one based on the installed framework version during CI matrix runs.

---

### Change 2 — `.github/workflows/tests.yml`

**What changes**:
```yaml
# Before
matrix:
  php: [8.2, 8.3]
  laravel: [11.*]

# After
matrix:
  php: [8.2, 8.3, 8.4]
  laravel: [11.*, 12.*]
```

**Jobs produced**: 6 (3 PHP × 2 Laravel versions). All other workflow steps remain unchanged.

---

### Change 3 — `README.md`

**What changes**: Update the requirements/compatibility section and any version badges to reflect:
- Laravel: `^11.0 | ^12.0`
- PHP: `^8.2` (8.2, 8.3, 8.4)

---

## Complexity Tracking

No constitution violations. This is a minimal, surgically scoped change:

| Risk | Assessment |
|------|-----------|
| Source code breakage | None — code audit confirms no deprecated PHP or Laravel APIs used |
| Laravel 11 regression | None — OR-constraint is purely additive |
| Test suite changes | None — existing tests work as-is on all 6 matrix cells |
| Pest 2→3 migration | Low — DSL API stable, no test rewrites needed |
